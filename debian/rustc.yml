FROM debian:testing

COPY prepare.sh cleanup.sh rustc.sh /root/

RUN apt update -yqq && apt install -yqq curl wget
RUN /usr/bin/sh /root/prepare.sh && \
    /usr/bin/sh /root/rustc.sh && \
    /usr/bin/sh /root/cleanup.sh
 
